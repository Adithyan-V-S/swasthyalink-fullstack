rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all authenticated users (including anonymous) to read/write
    // This is for development/admin purposes
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
